import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  boxTitle: {
    alignItems: "center",
  },
  textTitle: {
    color: "#FFFFFF",
    fontSize: 14,
    fontWeight: "bold",
    fontStyle: "italic",
  },
  textSubtitle: {
    color: "#FFFFFF",
    fontSize: 18,
    fontWeight: "bold",
    fontStyle: "italic",
  },
  boxLogo: {
    flexDirection: "row",
    alignItems: "center",
  },
  logo: {
    width: 80,
    height: 80,
  },
});

export default styles;
